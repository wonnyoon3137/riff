# Exec Plan 0002 — Riff v2 실행계획 (초안)

| 항목 | 내용 |
|---|---|
| 상태 | active (사람 검토 완료 — M1 P0 착수 승인, R5=시도까지만 확정) |
| 작성일 | 2026-06-12 |
| 담당 | planner → frontend-dev / backend-dev → qa-engineer |
| 출처 | v0.1 회고(`docs/retrospective/v0.1-harness-retrospective.md`) + GitHub 이슈 #1~#20 |

> 실행 계획은 일급 산출물이다(OpenAI harness 원칙). 진행/완료/의사결정 로그를 이 파일에 누적한다.
> 본 계획은 v0.1 회고에서 도출한 20개 이슈를 트랙으로 묶고 착수 순서를 정한다. 코드는 포함하지 않는다.

---

## 목표

v0.1에서 미룬 결정·부채·프로세스 개선을 갚아 v0.2 기능(검색·공연장 DB)의 토대를 만든다. 우선순위:
1. **선결 부채 해소** — KOPIS 실측(T-01) / 지역 병합 검증(T-02)이 다른 모든 KOPIS 작업의 전제.
2. **사용자 가치 + 무릎앞 개선** — D3 토스트(F-3/T-08), 린트 인코딩(P-2/F-4).
3. **기능** — 공연장 동기화 DB(F-2/D5), 공연명 검색(F-1/D10).
4. **프로세스 영속화** — E2E(P-1), PR 워크플로(P-6), 드리프트 가드(P-4/T-09).

---

## 관련 결정 (D1~D10 영향)

| 결정 | 영향 이슈 | v2에서 변경 여부 |
|---|---|---|
| D3 (31일 UI 제한) | F-3(#10), T-08(#19) | **불변.** 구현만 alert→토스트로 교체. 결정 자체는 유지 |
| D4 (지역 다중 선택) | T-02(#13), T-03(#14), T-04(#15) | **불변.** 병합 정확도/부하 실측 + 구군 데이터화로 D4 구현 완성 |
| D5 (공연장 사전 동기화) | F-2(#9), T-04(#15), T-05(#16) | **불변.** 스텁 해소 = D5의 실구현. 동기화 주기(T-05)만 운영값 확정 |
| D10 (검색 v0.1 제외) | F-1(#8) | **갱신 필요.** v0.2 검색 도입 시 `features.md` D10/F5를 먼저 갱신해야 함(아래 리스크 R1) |

---

## D1~D10 충돌 / 리스크 검토 결과

docs와 20개 이슈를 대조한 결과 **D 결정과 정면 충돌하는 이슈는 없다.** 단, 진행 전 처리해야 할 게이트 4건:

- **R1 (게이트) — F-1(#8) 검색은 D10 갱신 선행 필수.**
  D10 = "검색 v0.1 제외", features.md F5 = "v0.2 이관". 이슈 본문도 "features.md D10 갱신 선행"을 명시. KOPIS `shprfnm` 파라미터는 검증됨(kopis-integration §2.1). **코드 착수 전 `features.md`의 D10/F5를 먼저 갱신**해야 한다(결정 변경은 문서 먼저 원칙). 갱신 없이 검색 구현 시작 = 범위 위반.

- **R2 — F-2(#9)는 T-01·T-02·T-04에 의존.** D5 동기화 DB 구현(prfplc 전량 fetch)은 KOPIS rate limit(T-01)과 라벨→코드 역매핑(T-04)이 선결되어야 안전하다. F-2를 T-01/T-04보다 먼저 착수하면 동기화 중 rate limit에 막히거나 sidoCode/gugunCode가 비는 불완전 DB가 된다.

- **R3 — F-3(#10) ↔ T-08(#19)는 사실상 동일 작업.** 둘 다 `PeriodFilter.tsx:74`의 `alert("최대 31일까지 선택 가능합니다")` 교체. screens.md §139·§186에 토스트 스펙(하단중앙/좌하단, 3초, inverse-surface, 문구)이 이미 확정돼 있어 신규 결정 불필요. **하나의 작업으로 합쳐 처리**한다. 토스트 도입 후 `no-alert` 린트 유지(P-2/#2 연계).

- **R4 — T-03(#14) 구군 데이터화는 D4 구군 드릴다운의 전제.** kopis-codes §4.2가 "구군 표는 PDF/행정표준코드에서 생성"으로 미완 표기. 구군 단위 필터(features F2.2 2단계)를 v2에 넣으려면 선결. 단 kopis-codes §4 주석대로 "시도까지만 v0.1, 구군 v0.2 축소"가 가능하므로 **구군 범위 포함 여부는 제품 결정**(아래 R5).

- **R5 (제품 결정 요청) — 구군 드릴다운을 v2 범위에 포함할지.** 포함 시 T-03(#14) 데이터화 + T-04(#15) 역매핑이 선결로 들어온다. planner는 추측하지 않고 사람 결정을 요청한다.

---

## 트랙 분해 (이슈 → 트랙 매핑)

### 트랙 0 — 측정·검증 기반 (선결, 차단요소)
| 이슈 | 제목 | type/우선순위 | 담당 | 의존 |
|---|---|---|---|---|
| #12 T-01 | KOPIS rate limit/가동률 실측 | tech-debt/P0 | backend-dev | 없음 |
| #13 T-02 | 지역 다중 병합 페이지네이션 정확도/부하 검증 | tech-debt/P0 | backend-dev | 없음(병행 가능) |

> 이 트랙은 KOPIS를 건드리는 모든 후속 작업(F-2, 캐싱 T-06, 동기화 T-05)의 전제다. 가장 먼저 착수.

#### 측정 노트 (2026-06-12, backend-dev)

**#12 T-01 — rate limit/가동률 실측: 완료 (2026-06-12, 키 주입 후 실측)**
- 하네스 `scripts/measure/rate-limit-probe.ts` 를 실 서비스키(`.env`)로 실행. 단계별 30req, rows=10, timeout=5s.

| 동시성 | 성공률 | 처리량(req/s) | 지연 p50/p95/max(ms) | 비고 |
|---|---|---|---|---|
| 1 | 100% | 6.2 | 157 / 203 / 320 | 안정 |
| 2 | 100% | 10.7 | 186 / 246 / 302 | 안정 |
| **5** | **100%** | **11.2** | 361 / 820 / 827 | 처리량 정점, 지연 증가 시작 → **안전 상한** |
| 10 | 100% | 10.0 | 798 / 1946 / 3002 | 지연 급증(max≈timeout), 처리량 정체 |
| 20 | **63.3%** | 15.0 | 484 / 1867 / 1990 | **HTTP 400 11건 — 포화/거부 시작** |

- **결론:** 안전 동시성 상한 = **5**(100% 성공 + 처리량 정점, 지연 수용 가능). 10은 지연만 악화되고 처리량 이득 없음. 20에서 명확히 포화(성공률 63%). → 다중지역 병합 batch 동시성 기본값 5(§7.2)는 실측과 일치. `KOPIS_REGION_CONCURRENCY` 로 조정 가능하나 5 권장.
- **F-2(공연장 전량 동기화) 함의:** prfplc 대량 fetch도 동시성 ≤5로 batch + 재시도. 동기화 주기(T-05/#16)는 이 처리량(≈11 req/s @ c5)으로 산정.
- ⏳ **후속(권한 필요):** 이 수치를 `kopis-integration` 스킬 §호출전략/§에러·캐싱("rate limit 미검증→실측")에 반영해야 함. 스킬 문서는 에이전트 설정이라 자동수정 차단됨 → 사람 승인 후 갱신.

**#13 T-02 — 지역 다중 병합 페이지네이션 정확도/부하: 검증 완료, 결함 검출**
- 범위(R5): **시도 레벨 병합만** 검증. 구군은 v0.2 보류 → 대상 아님.
- 검증 도구(서비스키 불필요, 결정적 합성 데이터):
  - 하네스 `scripts/measure/region-merge-verify.ts` (`pnpm tsx ...`, 결함 시 exit 2).
  - 회귀 테스트 `src/__tests__/region-merge-pagination.test.ts` (vitest, 그린).
- **정확도 결과(결함):** 현행 `src/app/api/performances/route.ts` 의 다중지역 경로는 각 지역을 `cpage=page` 로 호출하고 병합 후 항상 `slicePage(merged, 1, rows)` 한다. 지역별 페이지 경계가 전역 정렬 병합 경계와 어긋나 **클라이언트 page>1 에서 누락 발생**(검증 시나리오: 지역 3개×80건, rows=30, page 2~4에서 각 30건 누락 = 누락 90건). 즉 무한스크롤 다음 페이지에서 항목이 통째로 빠진다(R5 시도 병합에서도 재현).
- **부하 결과:** 호출 수 = 페이지당 지역 수 N (O(N)). 시도 최대 17개 → 페이지당 ≤17 병렬 호출. §7.2 동시성 상한(기본 5)으로 batch 분할 필요. 실 지연/거부 임계는 #12 키 확보 후 실측으로 보정.
- **수정안(F-2 착수 전 적용 권장):** 다중지역은 각 지역을 `cpage=1..page`(또는 `page*rows` 충분량) 누적 fetch → 전역 병합·dedup·정렬 → `slicePage(merged, page, rows)` 로 전역 오프셋 슬라이스. 제안 알고리즘은 모든 페이지에서 ground-truth 와 일치함을 하네스·테스트로 확인.
- ✅ **수정 완료(2026-06-12, #21):** 위 수정안 적용. `src/server/kopis/concurrency.ts`(`mapWithConcurrency`, 상한 5) 신규 + `route.ts` 다중지역 경로 재작성. `region-merge-verify.ts` exit 0(전 페이지 누락 0/중복 0), `region-merge-pagination.test.ts` page>1 누락 0 단언 그린. vitest 38 pass.

### 트랙 1 — 즉시 가치 / UX·품질 (선행 의존 없음)
| 이슈 | 제목 | type/우선순위 | 담당 | 의존 |
|---|---|---|---|---|
| #10 F-3 + #19 T-08 | D3 alert→인라인 토스트 | feature·tech-debt/P0 | frontend-dev | 없음 (screens 스펙 확정됨) |
| #2 P-2 | 취향 린트 인코딩(a11y 확장, hex 금지) | process/P0 | frontend-dev | 없음 (no-alert는 #10 토스트 후 강제 가능) |
| #11 F-4 | ESLint CLI 마이그레이션(Next 16 대응) | feature/P2 | frontend-dev | P-2와 같은 린트 설정 영역 → 묶어서 |

### 트랙 2 — 공연장 동기화 DB (D5 스텁 해소) — M2 세부 계획

| 이슈 | 제목 | type/우선순위 | 담당 | 의존 |
|---|---|---|---|---|
| #15 T-04 | 시설 **시도** 라벨→코드 역매핑 (R5: 구군 보류) | tech-debt/P1 | backend-dev | 없음(시도표 §3 확정) |
| #16 T-05 | 공연장 동기화 주기 확정 | tech-debt/P2 | backend-dev(+**사람**) | #12(실측 완료) |
| #9 F-2 | 공연장 동기화 DB + /api/venues 실연동 | feature/P0 | backend-dev | #12✅, #13✅, #15, #16, **DEC-A 저장소 결정** |

> **M2 전제 상태:** #12(rate limit 실측, 상한 5)·#13(병합 검증)·#21(병합 누락 수정)은 PR #26으로 완료. R5(시도까지만)는 사람 결정 확정. 남은 게이트는 **DEC-A(저장소 선택)**·**DEC-B(동기화 주기)** 2건의 사람 결정뿐.

#### 트랙2-A. F-2 아키텍처 결정 추출

기존 코드/의존성 확인 결과:
- **DB 의존성 0.** `package.json` dependencies = next/react/react-query/fast-xml-parser/zod. ORM·DB 드라이버·SQLite 클라이언트 없음. `node_modules` 미설치 상태(스캐폴딩 매니페스트).
- **타깃 구조는 docs에 이미 명세됨.** `kopis-integration.md §4.2`: `server/venues/{sync,repo}.ts`, `app/api/venues/route.ts`(자동완성), `POST /internal/sync-venues`(동기화 트리거). `data-model.md §5.2`: `venues` 테이블 스키마 확정(아래 트랙2-D). → **신규 아키텍처 설계가 아니라 docs 명세의 구현.**
- **현 스텁:** `src/app/api/venues/route.ts` — `q<2`면 빈 배열, 그 외 `TODO: name_normalized LIKE %q%` 자리. `VenueFilter.tsx:85`가 이 API를 호출(계약은 `{items:[]}` 고정).
- **env 준비됨:** `.env`에 `VENUE_SYNC_CRON=`(빈 값, "운영 결정 필요(후보 주1회)" 주석). `KOPIS_*` 키 주입 완료(#12 단계).

도출된 **사람/설계 판단 필요 항목**:

| ID | 결정 항목 | 상태 | 비고 |
|---|---|---|---|
| **DEC-A** | 저장소 선택 (SQLite 파일 vs Postgres vs JSON 스냅샷) | **사람 결정 필요** | 아래 트랙2-B |
| **DEC-B** | 동기화 주기 (#16 T-05) | **사람 결정 필요** | 아래 트랙2-C |
| DEC-C | 동기화 메커니즘 (빌드타임/런타임 cron/수동) | planner 추천 → 사람 승인 | DEC-A·B에 종속 |
| DEC-D | 좌표/주소(상세) 보강 범위 | planner 결정(보류 권장) | D7 지도 제외 → v0.2. 자동완성에 불필요 |
| DEC-E | 자동완성 매칭(LIKE %q% vs prefix) | planner 결정 | §5.4 명세대로 contains, 상위 20 |

> DEC-D: features D7(지도)=제외, data-model §5.2 lat/lng=nullable·v0.1 미사용. 자동완성은 `fcltynm`/`sidonm`/`gugunnm`(목록 응답)만으로 충분 → **상세(/prfplc/{mt10id}) 좌표·주소 보강은 M2 범위에서 제외**(전량 동기화 시 N회 추가 호출=부하 2배). 보강은 지도 도입(v0.2) 시. backend-dev는 목록 응답만으로 동기화한다.

#### 트랙2-B. DEC-A 저장소 선택 (사람 결정)

후보(현실성 순):

| 후보 | 장점 | 단점 | 적합도 |
|---|---|---|---|
| **SQLite 파일 (better-sqlite3)** | 의존성 1개, 서버리스, `name_normalized` 인덱스/LIKE 네이티브, upsert(`ON CONFLICT`) 지원, 소규모(수천 행)에 충분 | 배포 환경이 FS 쓰기 가능해야 함(서버리스 read-only FS 시 빌드타임 생성 필요) | **권장** |
| JSON 스냅샷 (빌드타임 생성, 메모리 로드) | 의존성 0, 배포 단순, read-only FS 호환 | 런타임 갱신 불가(재배포 필요), LIKE는 JS 필터(수천 행이면 OK) | 차선(주기갱신 포기 시) |
| Postgres (외부) | 확장성, 동시쓰기 | 인프라/커넥션 운영비, v0.2 규모엔 과함 | v0.2 자체 인덱스 확장 시 재검토 |

planner 추천: **SQLite(better-sqlite3) 파일 DB.** 근거 — (1) 데이터가 작고(공연시설 전국 수천 건 추정) 읽기 위주 단일 노드, (2) §5.2 스키마/인덱스/LIKE/upsert를 추가 인프라 없이 충족, (3) v0.2 "자체 인덱스 기반" 확장 시 FTS5로 자연 확장. **배포 타깃 FS 쓰기 가능 여부**만 사람이 확인하면 결정 가능. read-only FS면 JSON 스냅샷(빌드타임)으로 폴백.

#### 트랙2-C. DEC-B 동기화 주기 (#16 T-05, 사람 결정)

부하 산정(실측 기반):
- 처리량 ≈ **11 req/s @ 동시성 5**(#12 실측). prfplc 목록 `rows=100`.
- 전국 공연시설 총 건수 = **미상(KOPIS 미조회)**. 보수적 가정 2,000~5,000건 → 페이지 20~50개.
- 전량 동기화 1회 = 20~50 req → **약 2~5초**(목록만, DEC-D로 상세 호출 제외). 부하·소요 모두 무시할 수준.
- ∴ 주기는 **부하가 아니라 데이터 변동성**으로 결정. 공연시설(건물)은 개·폐관이 드물어 변동성 낮음(§5.3 "변동 드물어 일/주 단위 충분").

옵션(구체 수치):

| 옵션 | cron | 근거 | planner 평가 |
|---|---|---|---|
| 주 1회 | `0 4 * * 1` (월 04:00 KST) | 변동성 낮음+신규 개관 1주 내 반영. 부하 무시 가능 | **권장(.env 주석 후보와 일치)** |
| 월 1회 | `0 4 1 * *` | 변동성 더 낮게 봄. 신규 시설 반영 지연(최대 1달) | 보수적, 가능 |
| 수동 스크립트 | (cron 없음) | 운영 단순, 누락 위험(사람이 잊음) | 비권장(운영 부채) |

planner 추천: **주 1회(`0 4 * * 1`)** + **수동 트리거 병행**(`POST /internal/sync-venues`로 즉시 동기화 가능하게). 근거 — 변동성 대비 적정, 부하 무시 가능, 신규 시설 1주 내 반영. `VENUE_SYNC_CRON`에 이 값 기입.

> 메커니즘(DEC-C): DEC-A=SQLite(런타임 쓰기) 채택 시 → 런타임 cron(또는 호스팅 스케줄러)이 `POST /internal/sync-venues` 호출. SQLite 불가(read-only FS)·JSON 폴백 시 → 빌드타임 1회 생성 스크립트(주기갱신은 재배포). **DEC-A 확정 후 자동 결정.**

#### 트랙2-D. 공연장 스키마 초안 (data-model §5.2 확정 반영, M2 구현 대상)

`venues` 테이블 (SQLite 기준 DDL 초안 — 구현은 backend-dev):

| 컬럼 | 타입 | 출처/비고 |
|---|---|---|
| `id` PK | TEXT | `mt10id` (예 FC001431) |
| `name` | TEXT | `fcltynm` (trim) |
| `name_normalized` | TEXT | 공백/대소문자 제거 — 자동완성 매칭, **INDEX** |
| `sido_name` | TEXT | `sidonm` 라벨(목록) ✅ |
| `gugun_name` | TEXT | `gugunnm` 라벨(목록) ✅ |
| `sido_code` | TEXT NULL | 라벨→코드 파생(#15 T-04, kopis-codes §3) |
| `gugun_code` | TEXT NULL | **R5: 보류(항상 NULL).** 컬럼만 유지, 채우지 않음 |
| `hall_count` | INTEGER NULL | `mt13cnt` |
| `open_year` | TEXT NULL | `opende` |
| `address` | TEXT NULL | DEC-D로 M2 미보강(NULL) |
| `lat`/`lng` | REAL NULL | DEC-D로 M2 미보강(NULL) |
| `synced_at` | TEXT | ISO datetime |

인덱스: `name_normalized`(contains 검색), `id` unique. 정규화는 kopis-integration 스킬 `normalize`/`toVenue()` 경계에서 수행.

#### 트랙2-E. #15 T-04 시도 역매핑 (구군 제외)

- **범위(R5 확정):** **시도 라벨→코드만.** 구군(`gugunnm`→`gugun_code`) 역매핑 보류 → `gugun_code` 항상 NULL.
- **데이터 출처:** `kopis-codes.md §3` — 17개 시도 `signgucode`(행정표준 앞2자리) ✅ 공식 확정. 코드값 하드코딩 허용(검증값).
- **매핑 난점:** 응답 `sidonm`은 **축약 라벨**(예 "경북", "서울") 가능, §3 표는 **정식 명칭**(예 "경상북도", "서울특별시"). 역매핑 시 축약↔정식 정규화 테이블 필요(예 "경북"→47, "서울"→11). backend-dev는 17개 시도 별칭(축약/정식) 매핑을 작성하고, 매칭 실패 라벨은 `sido_code=NULL`로 두고 로그(데이터 품질 관측).
- **금지:** 구군 역매핑(별칭 충돌·전체 표 미데이터화 #14 보류). `gugun_code`는 컬럼만.
- **검증(qa):** 동기화된 venues 전수에서 `sido_code` NULL 비율 측정(목표: 매칭 실패 0건에 근접). 18개 코드값이 §3과 일치.

#### 트랙2-F. 착수 순서 & 담당 (backend-dev 주도)

1. **[게이트] DEC-A·DEC-B 사람 결정 수집** (planner→사람). 미결정 시 착수 금지(추측 금지).
2. **#15 T-04 시도 역매핑 유틸** (backend-dev) — `server/kopis/normalize` 또는 `server/venues`에 시도 별칭→코드 맵 + `toVenue()` 코드 파생. **DEC-A 무관, 선착수 가능**(첫 작업).
3. **저장소 레이어** (backend-dev, DEC-A 확정 후) — `server/venues/repo.ts`(스키마 생성/upsert/`searchByName`). §5.2 스키마.
4. **동기화 로직** (backend-dev) — `server/venues/sync.ts`: prfplc 목록 전량 페이지네이션(동시성 ≤5 `mapWithConcurrency` 재사용, 5xx/timeout 1회 재시도) → `toVenue()`(시도 역매핑 포함) → upsert. DEC-D로 상세 호출 제외.
5. **트리거** (backend-dev, DEC-C 확정 후) — `POST /internal/sync-venues`(수동) + `VENUE_SYNC_CRON`(주기).
6. **`/api/venues` 실연동** (backend-dev) — 스텁 제거, `repo.searchByName(q, 20)` 연결. **응답 계약(`{items:[]}`)·`q<2` 가드 유지**(VenueFilter.tsx 무변경).
7. **qa 검증** (qa-engineer) — 아래.

**qa 검증 포인트:**
- 동기화: prfplc 전량 fetch 시 동시성 ≤5 준수(거부 0), 재시도 동작, upsert 멱등(2회 실행 시 행 수 동일).
- 데이터 품질: `sido_code` NULL 비율(역매핑 실패) 측정, `gugun_code` 전건 NULL(R5 준수), `mt10id` 중복 0.
- 자동완성 계약: `/api/venues?q=롤링` 실데이터 반환, `q<2` 빈 배열, 응답 형태 불변(VenueFilter 회귀 없음).
- E2E(#1 자산 재사용): 공연장 필터 입력→자동완성→선택→`prfplccd` 전달 플로우 그린.

**완료 기준:**
- `/api/venues`가 스텁이 아닌 자체 DB 실데이터 반환. VenueFilter 자동완성 실동작.
- venues 테이블 전량 동기화 1회 성공(멱등), 시도 역매핑 적용(NULL 비율 보고).
- `VENUE_SYNC_CRON` 운영값 기입 + 수동 트리거 동작.
- `pnpm typecheck && lint && test` 그린, qa E2E 통과. data-model §5.3 "확인 필요"·tech-debt TD-04/TD-05 상태 갱신.

**리스크:**
- DEC-A가 read-only FS면 런타임 주기갱신 불가 → JSON 빌드타임 폴백(주기=재배포). 배포 타깃 확인 선행.
- prfplc 총 건수 미상 → 페이지 수 가정. 첫 동기화 실행으로 실측·기록(이후 부하 재산정).
- 시도 축약 라벨 변형이 §3 별칭표에 없으면 `sido_code` NULL → 관측 후 별칭 보강(블로킹 아님).

### 트랙 3 — 지역 필터 구군 확장 (D4 완성, 제품 결정 R5 게이트)
| 이슈 | 제목 | type/우선순위 | 담당 | 의존 |
|---|---|---|---|---|
| #14 T-03 | 구군(signgucodesub) 전체 코드 데이터화 | tech-debt/P1 | backend-dev | 없음(데이터 생성) |
| (D4 구군 UI) | 구군 드릴다운 필터 | — | frontend-dev | #14, #13 |

> R5(구군 범위) 미결정 시 이 트랙은 보류. 결정되면 #14 선착수.

### 트랙 4 — 검색 (D10 갱신 게이트)
| 이슈 | 제목 | type/우선순위 | 담당 | 의존 |
|---|---|---|---|---|
| (선행) | features.md D10/F5 갱신 | — | planner | 없음 (R1 게이트) |
| #8 F-1 | 공연명 검색(shprfnm) | feature/P1 | backend-dev + frontend-dev | D10 갱신 선행 |

### 트랙 5 — 성능/메모리 튜닝 (저우선, 실측 후)
| 이슈 | 제목 | type/우선순위 | 담당 | 의존 |
|---|---|---|---|---|
| #17 T-06 | React Query 캐시 상한/gcTime 튜닝 | tech-debt/P2 | frontend-dev | #13(부하 실측 데이터 참조) |

### 트랙 6 — 프로세스 영속화 (병행 가능, 일부 P0)
| 이슈 | 제목 | type/우선순위 | 담당 | 의존 |
|---|---|---|---|---|
| #1 P-1 | E2E 핵심 플로우 3개 (D8 복원/D4 병합/필터→상세) | process/P0 | qa-engineer | 기존 v0.1 화면 (즉시 착수 가능) |
| #18 T-07 | 커스텀 린트(레이어 의존·hex·미검증코드) | tech-debt/P1 | backend-dev | P-2(#2)와 린트 영역 공유 |
| #4 P-4 + #20 T-09 | docs↔code 드리프트 가드 (존재 확인→일치 확인) | process·tech-debt/P1 | backend-dev | 없음 (CI 확장) |
| #6 P-6 | 기능 브랜치 + PR 워크플로 전환 | process/P1 | planner(+사람) | 없음 (이번 v2부터 적용) |
| #5 P-5 | QA 게이트 티어링(Fast/Deep) | process/P2 | qa-engineer | 없음 |
| #3 P-3 | orchestrator 위임 정책 검증 | process/P1 | planner | 1개 기능 완료 후 회고 |
| #7 P-7 | 하네스 ROI A/B 측정 | process/P2 | planner | 검색 등 1개 기능으로 측정 |

---

## 마일스톤 순서 제안

| 마일스톤 | 묶음 | 근거 |
|---|---|---|
| **M1 (지금 착수)** | 트랙 0(#12,#13) + 트랙 1(#10/#19, #2, #11) + #1(P-1 E2E) | 선행 의존 0, P0 다수, 측정 기반과 회귀 안전망을 먼저 깐다 |
| M2 | 트랙 2 공연장 DB(#15→#16→#9), 트랙 6 가드(#4/#20, #18) | M1의 rate limit 실측에 의존. D5 실구현 |
| M3 | 트랙 4 검색(D10 갱신→#8), 트랙 3 구군(R5 결정 시 #14) | 기능 확장. 문서 갱신 게이트 선행 |
| M4 | 트랙 5 튜닝(#17), 프로세스 회고(#3 위임검증, #7 ROI, #5 QA티어) | 실측·기능 완료 후 마무리 |

> #6(PR 워크플로)은 M1 착수와 동시에 프로세스로 적용(이후 모든 트랙이 PR 기반).

---

## 🎯 지금 당장 착수 추천 — M1 P0 묶음

**추천: 트랙 0(#12 T-01 rate limit 실측 + #13 T-02 병합 검증) + 트랙 1의 #10/#19(D3 토스트) + #1(E2E)**

근거:
1. **선행 의존이 0이다.** 네 작업 모두 다른 이슈를 기다리지 않는다. 반면 F-2(#9)·F-1(#8)은 각각 실측/문서갱신 게이트에 막혀 있다.
2. **#12·#13은 차단 해제(unblocker)다.** v2의 최대 기능 가치(F-2 공연장 DB)가 rate limit 실측 없이는 안전하게 못 들어간다. 가장 비싼 의존을 먼저 푼다.
3. **#10/#19는 가치/비용비가 최고다.** 위치(`PeriodFilter.tsx:74`)·스펙(screens §139·§186) 모두 확정, 신규 결정 0, 변경 1파일 수준. 접근성 부채(alert)를 즉시 갚고 P-2(#2) no-alert 린트의 전제를 만든다.
4. **#1 E2E는 안전망이다.** 이후 M2~M3에서 BFF·필터를 대거 손대기 전에 D8 복원/D4 병합/필터→상세 회귀 테스트를 깔아두면 리팩터링 리스크가 급감한다. 기존 v0.1 화면이 이미 있어 즉시 작성 가능.

**M1 담당 배분**
- backend-dev: #12, #13 (KOPIS 실측 — kopis-integration 스킬 적용, 결과를 §10 "확인 필요"에 반영)
- frontend-dev: #10+#19 (토스트 컴포넌트 + PeriodFilter 교체 — nextjs-patterns/M3 스낵바 스펙), 이어서 #2/#11 린트
- qa-engineer: #1 E2E 3종 (Playwright)

**M1 완료 기준**
- #12/#13: rate limit 실측 수치 + 병합 정확도/부하 결과를 docs(kopis-integration §10, data-model §6.2)에 기록. tech-debt-tracker TD-01/TD-02 상태 갱신.
- #10/#19: alert 제거, M3 스낵바로 교체, `no-alert` 린트 error 유지, `pnpm typecheck && lint && test` 그린.
- #1: E2E 3개 플로우 통과.

---

## 리스크

- **R1 D10 게이트:** 검색(#8) 착수 전 features.md 미갱신 시 범위 위반 → M3 진입 시 planner가 문서 먼저 갱신.
- **R2 F-2 의존 사슬:** #9가 #12/#13/#15/#16에 의존 → 의존 미충족 상태로 착수 금지.
- **R5 구군 범위 미결정:** 트랙 3은 제품 결정 전까지 보류(추측 금지).
- **KOPIS rate limit 실값:** #12 결과에 따라 동기화 동시성 상한·캐싱 TTL을 재조정해야 할 수 있음(T-05/T-06 영향).

---

## 진행 로그
- 2026-06-12 planner: 이슈 #1~#20 분해, D1~D10 충돌 검토(충돌 0, 게이트 4), 트랙 0~6 + M1~M4 마일스톤 작성. M1 P0 묶음 추천. 사람 검토 대기(R5 구군 범위 결정 요청 포함).
- 2026-06-12 planner: M2 트랙2(공연장 DB) 세부 계획 확장 — F-2 결정 추출(DEC-A~E), 저장소 후보 분석(SQLite 권장), 동기화 주기 옵션(주1회 권장), 스키마 초안(§5.2 반영), 시도 역매핑 범위(R5 구군 제외), 착수 순서(backend-dev 7단계)·qa 검증 포인트 작성. 기존 코드 확인: DB 의존성 0, 타깃 구조는 docs에 기명세. **사람 결정 2건(DEC-A 저장소·DEC-B 주기) 대기** — 결정 전 #9 구현 착수 금지. #15 역매핑 유틸은 게이트 무관 선착수 가능.

## 의사결정 로그
- 2026-06-12: F-3(#10)와 T-08(#19)을 단일 작업으로 통합(동일 위치·동일 스펙).
- 2026-06-12: 검색(#8)은 features.md D10/F5 갱신을 선행 게이트로 고정.
- 2026-06-12: 구군 드릴다운(트랙 3) 범위는 제품 결정(R5)까지 보류 — planner 추측 금지.
- 2026-06-12 **사람 결정**: R5 = **시도까지만, 구군 v0.2 보류**. 트랙 3(#14 T-03, 구군 UI) v2 범위에서 제외. D4는 시도 레벨 병합으로 완성 정의. → #15 T-04 역매핑은 "시도 라벨→코드"만 범위(구군 역매핑 보류).
- 2026-06-12 **사람 결정**: v2 첫 착수 = **M1 P0 묶음 전체** 승인 — #12·#13(backend-dev), #10/#19 토스트(frontend-dev), #1 E2E(qa-engineer) 병렬 착수.
- 2026-06-12 M1 완료: 토스트(#10/#19)·E2E 3종(#1)·측정 하네스(#12/#13) 그린 → **PR #26**. 검증 중 결함 5건 발견 → 이슈화(#21~#25).
- 2026-06-12 **사람 결정**: #21(병합 누락, P0) 즉시 수정 + KOPIS 키 주입. → #12 실측 완료(안전 동시성 상한=5), #21 수정 완료. PR #26에 통합.
- 2026-06-12 planner: M2 트랙2 DEC-D 결정 — 좌표/주소(상세 /prfplc/{mt10id}) 보강은 M2 제외(D7 지도 제외·자동완성 불필요·부하 2배). 목록 응답만으로 동기화. 보강은 v0.2 지도 도입 시.
- 2026-06-12 planner: M2 트랙2 DEC-E 결정 — 자동완성 매칭은 §5.4 명세대로 `name_normalized` contains(LIKE %q%) 상위 20, `q<2` 가드·`{items:[]}` 계약 유지(VenueFilter 무변경).
- 2026-06-12 **사람 결정 요청(미결)**: DEC-A 저장소 선택(planner 추천 SQLite, 배포 FS 쓰기 가능 시) / DEC-B 동기화 주기(planner 추천 주1회 `0 4 * * 1`+수동 트리거). 두 결정 확정 전 #9 F-2 구현 착수 금지.
